{
  "name": "selfconnect-ecosystem",
  "private": true,
  "version": "0.0.0",
  "description": "SelfConnect monorepo — TSK client, MCP server, and Python SDK",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  }
}
